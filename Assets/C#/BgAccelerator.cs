using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BgAccelerator : MonoBehaviour
{
    /*Rigidbody2D rb;
    float posX;
    float moveSpeed = 40f;
    void Start()
    {
        rb = GetComponent<Rigidbody2D>();
    }

    void Update()
    {
        posX = Input.acceleration.x * moveSpeed;
        transform.position = new Vector2(Mathf.Clamp(transform.position.x, -500f, 500f), transform.position.y);
    }

    private void FixedUpdate()
    {
        rb.velocity = new Vector2(posX, 0f);
    }*/   
}
